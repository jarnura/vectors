{
  "name": "graphics",
  "version": "1.0.0",
  "description": "vectors transformations, projection",
  "scripts": {
    "build": "spago bundle",
    "test": "spago test",
    "dev": "spago bundle && node dev-server.js",
    "e2e": "playwright test",
    "e2e:report": "playwright show-report artifacts/e2e-report",
    "docker:build": "docker build -t vectors:local .",
    "docker:run": "docker run --rm -p 8080:80 --name vectors vectors:local"
  },
  "author": "jarnura",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.28.0",
    "purescript": "^0.15.16",
    "purs-tidy": "^0.11.1",
    "spago": "^1.0.4"
  },
  "dependencies": {
    "@playwright/test": "^1.60.0",
    "animejs": "^4.4.1"
  }
}
